.icon {
  display: inline-block;
  width: 1em;
  height: 1em;
  fill: currentColor;
}

  {{#shapes}}
  .{{base}} {
    font-size: {{height.inner}}*1em;
    width: ({{width.inner}}/{{height.inner}})*1em;
  }
  {{/shapes}}
